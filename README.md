# Eslint config for Enspirit's vue projects

## How to use

Install the plugin:
```
npm install --save-dev @enspirit/eslint-config-vue
```

Edit your .eslintrc.js to include the plugin:

```
module.exports = {
  'extends': '@enspirit/eslint-config-vue'
};
```
